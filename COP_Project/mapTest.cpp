#include <iostream>
#include <unordered_map>
#include <hash_set>
#include <vector>

using namespace std;

int main()
{
    vector<int> v;
    cout << v.capacity();
    v.push_back(1);
    v.push_back(1);
v.push_back(1);
v[3] = 9;
    cout << " " << v.capacity();

map
    return 0;
}
